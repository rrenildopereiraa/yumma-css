module.exports = {
  content: [], // Specify paths to scan for class names.
  output: "", // Path for saving final CSS file.

  capabilities: {
    core: true, // Should base styles be included? (Default is false)
    minify: false, // Should I create a minified version? (Default is true)
  },
};
